DATABASE_URL=mongodb+srv://(USERNAME):(PASSWORD)@cluster0.qsirn.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
DATABASE_NAME=test
JWT_EXPIRE=
JWT_SECRET=

# CORS (optional; if not set, Nest uses defaults)
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# ALLOWED_METHODS=GET,HEAD,PUT,PATCH,POST,DELETE,OPTIONS
# ALLOWED_HEADERS=Content-Type,Authorization

# Encryption key for sensitive data (e.g. API keys, integration tokens). 64 hex chars or 44 base64 chars for 32 bytes.
# ENCRYPTION_KEY=